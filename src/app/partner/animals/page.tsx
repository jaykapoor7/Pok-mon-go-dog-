import { AnimalsClient } from "@/components/partner/AnimalsClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Animals — StrayPaw Partner" };

export default function PartnerAnimalsPage() {
  return <AnimalsClient />;
}
