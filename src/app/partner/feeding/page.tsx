import { FeedingClient } from "@/components/partner/FeedingClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Feeding zones - StrayPaw Partner" };

export default function PartnerFeedingPage() {
  return <FeedingClient />;
}
