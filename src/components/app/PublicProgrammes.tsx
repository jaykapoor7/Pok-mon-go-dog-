import Link from "next/link";
import { ArrowUpRight, CalendarCheck2, MapPin } from "lucide-react";
import type { PublicProgramme } from "@/lib/public-programmes";

export function PublicProgrammes({ programmes }: { programmes: PublicProgramme[] }) {
  if (!programmes.length) return null;
  return <section className="community-programmes" aria-labelledby="community-programmes-title"><div className="community-programmes-heading"><div><span className="product-kicker">Verified public work</span><h2 id="community-programmes-title">Completed drives, not vanity metrics.</h2><p>Only aggregate work an organisation intentionally publishes appears here. Individual records and informer details remain private.</p></div><Link href="/partners">Meet the partners <ArrowUpRight size={15} /></Link></div><div className="community-programme-grid">{programmes.map((programme) => <article key={programme.id}><div className="community-programme-top"><span><CalendarCheck2 size={15} /> Completed drive</span><Link href={programme.ngo_slug ? `/org/${programme.ngo_slug}` : "/partners"}>{programme.ngo_name}</Link></div><h3>{programme.name}</h3><p className="community-programme-place"><MapPin size={14} /> {[programme.zone, programme.city, programme.state].filter(Boolean).join(", ")}</p>{programme.public_summary && <p className="community-programme-summary">{programme.public_summary}</p>}<dl><div><dt>Records</dt><dd>{programme.animals_recorded.toLocaleString()}</dd></div>{programme.kind === "sterilisation" && <div><dt>Sterilised</dt><dd>{programme.sterilised_recorded.toLocaleString()}</dd></div>}{programme.kind === "vaccination" && <div><dt>Vaccinated</dt><dd>{programme.vaccinated_recorded.toLocaleString()}</dd></div>}</dl></article>)}</div></section>;
}
