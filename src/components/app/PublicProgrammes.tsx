import Link from "next/link";
import { ArrowUpRight, CalendarCheck2, MapPin } from "lucide-react";
import type { PublicProgramme } from "@/lib/public-programmes";

export function PublicProgrammes({ programmes }: { programmes: PublicProgramme[] }) {
  if (!programmes.length) return null;
  return <section className="community-programmes" aria-labelledby="community-programmes-title"><div className="community-programmes-heading"><div><span className="product-kicker">Public field work</span><h2 id="community-programmes-title">Work completed, shown clearly.</h2><p>See the drives and care work our partner organisations choose to share.</p></div><Link href="/partners">Meet the partners <ArrowUpRight size={15} /></Link></div><div className="community-programme-grid">{programmes.map((programme) => { const isSterilisation = programme.kind === "sterilisation"; const isVaccination = programme.kind === "vaccination"; const total = isSterilisation ? programme.sterilised_recorded : isVaccination ? programme.vaccinated_recorded : programme.animals_recorded; return <article key={programme.id}><div className="community-programme-top"><span><CalendarCheck2 size={15} /> Completed drive</span><Link href={programme.ngo_slug ? `/org/${programme.ngo_slug}` : "/partners"}>{programme.ngo_name}</Link></div><h3>{programme.name}</h3><p className="community-programme-place"><MapPin size={14} /> {[programme.zone, programme.city, programme.state].filter(Boolean).join(", ")}</p>{programme.public_summary && <p className="community-programme-summary">{programme.public_summary}</p>}<dl><div><dt>{isSterilisation ? "Dogs sterilised" : isVaccination ? "Animals vaccinated" : "Animals helped"}</dt><dd>{total.toLocaleString()}</dd></div></dl></article>; })}</div></section>;
}
